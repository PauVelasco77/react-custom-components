import { Canvas, Meta } from "@storybook/blocks";
import Example from "./example.tsx";
import * as Title from "./title.stories.jsx";
import React from "react";

<Meta of={Title} title="Title" />

# Title

The `Title` component is used to display a title with various configurable properties.

## Examples

### Default Example

<Canvas of={Title.Primary} />

## Usage

To use the `Title` component in your project, import it from the components library and use it as shown below:

```tsx
import { Title } from "components-library";

const Example = () => {
  return (
    <Title
      text="Title"
    />
  );
};

export default Example;
